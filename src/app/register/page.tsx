import RegisterForm from "@/features/authPages/register";

export default function RegisterPage() {
	return (
		<main className="flex flex-col items-center justify-center w-screen h-screen">
			<RegisterForm />
		</main>
	);
}
